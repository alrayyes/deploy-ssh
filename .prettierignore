# Prettier is here to fill Biome's gaps, which today means Markdown and YAML.
# Nothing else: Biome has the JSON. Everything below is either somebody else's
# file or a format Prettier has no business reformatting, and listing them stops
# its presence being mistaken for permission to touch them.
node_modules/
.bun-cache/
bun.lock

# Written by the release job, not by hand.
CHANGELOG.md

# The Contributor Covenant, verbatim. Rewrapping it would be editing somebody
# else's document and would make the diff against upstream useless.
CODE_OF_CONDUCT.md

# The GNU licence text, exactly as the FSF publishes it.
LICENSE

# Vale's downloaded style packages.
styles/Google/
styles/proselint/

# Generated by `openspec init`: the OpenSpec CLI's own skill/command docs and
# every change's proposal/specs/tasks, both held to OpenSpec's own template
# contract, not this repo's Markdown style.
.claude/
openspec/
